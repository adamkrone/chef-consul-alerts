description "Consul Alerts Service (Upstart)"

start on (local-filesystems and net-device-up IFACE!=lo)
stop on runlevel [06]

env GOMAXPROCS=${GOMAXPROCS}
env PATH=$PATH:/opt/consul/<%= node['consul']['version'] %>/

exec <%= @command %> <%= @options %>

respawn
respawn limit 10 10
